<div class="body-homepage">
  </div>
  <div class="home-space">
    <div class="slogan-home text-center text-white">
      <h2>La colocation <strong>solidaire </strong>qui <strong>unit</strong> les générations</h2>
    </div>
    <div class="d-flex justify-content-around home-space-content ">
        <div class="junior-home-space text-white d-flex align-items-end text-center">
          <p class="m-0 p-1"> Alicia, 20 ans loue depuis 1 an  une chambre à 100€ par mois chez un senior</p>
        </div>

        <div>
          <div class="text-center">
            <h4>Plus de <strong><%= @flats.length %></strong> logements disponibles</h4>
          </div>
          <div class="p-0 map-home-space align-self-center">
            <div style="width: 100%; height: 250px;"
                data-controller="map"
                data-map-markers-value="<%= @markers.to_json %>"
                data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>">
            </div>
          </div>
        </div>

        <div class="senior-home-space text-white d-flex align-items-end text-center">
          <p class="m-0 p-1"> Michel accueille depuis 3 ans des étudiants dans sa maison en banlieue de Paris</p>
        </div>




    </div>
    <div class="homespace-login d-flex justify-content-around">
        <% if user_signed_in? %>
          <div class="text-white">
            <%= link_to "Mon bureau", pages_junior_dashboard_path, class: "" if current_user.role == "junior"%>
            <%= link_to "Mon bureau", pages_senior_dashboard_path, class: "" if current_user.role == "senior"%>
          </div>
        <% else %>
          <div class="text-white">
            <%= link_to "Je me connecte", new_user_session_path, class: "" %>
          </div>
        <% end %>
    </div>
        <% if user_signed_in? %>
          <div class="d-flex justify-content-around mt-3">

          </div>
        <% else %>
           <div class="d-flex justify-content-around mt-3">
            <%= link_to "Rejoins nous!", new_user_registration_path, class: "btn btn-primary text-white"%>
          </div>
        <% end %>
  </div>
